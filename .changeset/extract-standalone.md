---
"@vorionsys/car-cli": patch
---

Extract `car-cli` into its own standalone repository (`vorionsys/car-cli`) with tokenless OIDC trusted publishing + provenance. Depends on `@vorionsys/car-client@^1.0.0` (resolved from npm). Relicensed Apache-2.0.
